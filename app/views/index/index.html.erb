<% content_for :meta_title, t('.title') %>

<article>
  <%= render partial: 'index/index/editable', locals: { editable_page: @editable_page } %>

  <%= render partial: 'index/index/recent_posts', locals: { collection: Post.list_for_visitors.first(6) } %>
</article>
